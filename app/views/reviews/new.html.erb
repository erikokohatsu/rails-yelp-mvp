<h1>Add a review</h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>
<p>Rating</p>
<%= f.select :rating, collection: Review::RATING %>
<%= f.input :content %>

<%= f.button :submit %>
<% end %>
